node_modules/
dist/
*.ehpk
.env
.DS_Store

# Neural speaker-embedding models (downloaded on the server, not committed)
models/
*.onnx

# Local scratch: test recordings, diagnostic captures (personal audio — never commit)
tmp/
*.diarize.log
